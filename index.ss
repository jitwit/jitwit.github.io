
(define page
  `(html
    (head
     (*css*)
     (meta (@ (charset "UTF-8")))
     (title "jitwit"))
    (body
     (section 2 "Posts")
     (section 3 "Other")
     (link "Benchmarks" "benchmarks/index.html"))
    (footer)))
